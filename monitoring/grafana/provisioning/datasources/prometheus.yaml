apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Stable uid so provisioned dashboards can bind to this datasource
    # deterministically instead of relying on an auto-generated uid.
    uid: aetheris-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
