---
phase: 01-evidence-graph-decision-snapshots-completeness
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/src/axum_routers/memory.rs
  - backend/src/axum_routers/mod.rs
  - backend/src/models/evidence.rs
  - backend/src/services/evidence_graph.rs
  - backend/tests/evidence_api.rs
  - backend/tests/snapshot_export.rs
  - docs/ARCHITECTURE.md
  - docs/evidence_graph.md
autonomous: true
requirements:
  - EVID-04
  - EVID-05
  - COMP-01
must_haves:
  truths:
    - "Users can retrieve workflow evidence through the live Axum router at GET /api/v1/workflows/{id}/evidence."
    - "The returned payload is a deterministic, versioned snapshot suitable for offline audit review."
    - "Architecture documentation explains the evidence model, integrity guarantees, and EU AI Act reporting usage without over-claiming compliance."
  artifacts:
    - "backend/src/axum_routers/memory.rs exposes the workflow evidence endpoint on the live router tree."
    - "backend/tests/evidence_api.rs and backend/tests/snapshot_export.rs prove the endpoint contract and export stability."
    - "docs/ARCHITECTURE.md and docs/evidence_graph.md document model, guarantees, limits, and reporting examples."
  key_links:
    - "backend/src/axum_routers/memory.rs calls backend/src/services/evidence_graph.rs to assemble the response."
    - "backend/src/axum_routers/mod.rs registers the new path with utoipa so docs track the live endpoint."
    - "The docs reference the exact implemented endpoint and payload fields, not aspirational future behavior."
---

<objective>
Expose the evidence graph through the live audit API, guarantee deterministic export payloads, and close the documentation requirement for Phase 01.

Purpose: Ship an audit-ready retrieval surface and the supporting documentation that matches the real implementation.
Output: `GET /api/v1/workflows/{id}/evidence` on the booted Axum router, deterministic export serialization, API tests, snapshot-export tests, and updated architecture docs.
</objective>

<execution_context>
@/Users/jiafan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jiafan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
@backend/src/axum_routers/memory.rs
@backend/src/axum_routers/mod.rs
@backend/src/services/evidence_graph.rs
@docs/ARCHITECTURE.md
</context>

Router reconciliation note: `CLAUDE.md` gives the generic endpoint convention as `backend/src/routers/*`, but Phase 01 must follow the live server surface documented in `01-RESEARCH.md`: `backend/src/main.rs` boots `axum_routers::create_router()`. Implement this plan only under `backend/src/axum_routers/*` and do not add a duplicate evidence handler under the legacy `backend/src/routers/*` tree.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the live workflow evidence endpoint and API contract tests</name>
  <read_first>
    - backend/src/axum_routers/memory.rs
    - backend/src/axum_routers/mod.rs
    - backend/src/services/evidence_graph.rs
    - backend/src/main.rs
    - backend/tests/evidence_api.rs
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
  </read_first>
  <files>backend/src/axum_routers/memory.rs, backend/src/axum_routers/mod.rs, backend/tests/evidence_api.rs</files>
  <behavior>
    - Test 1: `GET /api/v1/workflows/{id}/evidence` returns 200 with workflow metadata, nodes, edges, and verification status for a known run.
    - Test 2: Missing workflow id returns the repository/service not-found error through `AppError`, not a placeholder `"{}"` or `"[]"` string.
    - Test 3: The new handler is registered in the live Axum OpenAPI document because `main.rs` boots `axum_routers::create_router()`.
  </behavior>
  <action>Start by verifying that `backend/src/main.rs` boots `axum_routers::create_router()`. That check is the explicit reconciliation for the generic `backend/src/routers/*` endpoint convention in `CLAUDE.md`: for this phase, implement `get_workflow_evidence` only in `backend/src/axum_routers/memory.rs` using typed `Path` and `Query` extractors plus the evidence service from Plan 02, then register it on the live router path `GET /api/v1/workflows/{id}/evidence` and include the handler in `backend/src/axum_routers/mod.rs` OpenAPI path registration. Return `JsonResult<WorkflowEvidenceResponse>` with stable field names from `backend/src/models/evidence.rs`; do not add a duplicate evidence handler under `backend/src/routers`, because that tree is not booted by `main.rs`.</action>
  <acceptance_criteria>
    - `rg -n "axum_routers::create_router" backend/src/main.rs`
    - `rg -n "get_workflow_evidence|/api/v1/workflows/\\{id\\}/evidence" backend/src/axum_routers/memory.rs backend/src/axum_routers/mod.rs`
    - `rg -n "Path<.*workflow|Query<.*Evidence" backend/src/axum_routers/memory.rs`
    - `! rg -n "get_workflow_evidence|/api/v1/workflows/\\{id\\}/evidence" backend/src/routers`
    - `rg -n "GET /api/v1/workflows/\\{id\\}/evidence|workflow evidence" backend/tests/evidence_api.rs`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo test --test evidence_api</automated>
  </verify>
  <done>The live backend exposes the required evidence retrieval endpoint and automated tests prove it is wired to the router that the server actually serves.</done>
</task>

<task type="auto">
  <name>Task 2: Make evidence exports deterministic and prove offline snapshot completeness</name>
  <read_first>
    - backend/src/services/evidence_graph.rs
    - backend/src/models/evidence.rs
    - backend/tests/snapshot_export.rs
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
  </read_first>
  <files>backend/src/models/evidence.rs, backend/src/services/evidence_graph.rs, backend/tests/snapshot_export.rs</files>
  <action>Update the export contract in `backend/src/models/evidence.rs` so `WorkflowEvidenceExport` explicitly carries `schema_version`, `hash_algorithm`, `workflow_id`, `attempt_id`, `root_hash`, `chain_verified`, `nodes`, `edges`, and `exported_at`; if `WorkflowEvidenceResponse` wraps that export payload, update its field definitions in the same file at the same time so the API contract and export contract stay aligned. Then extend `backend/src/services/evidence_graph.rs` to build that deterministic, versioned snapshot for offline review. Use explicit field ordering or ordered maps so re-exporting the same stored workflow produces identical bytes apart from `exported_at`; if `exported_at` prevents byte-for-byte equivalence, isolate it outside the hashed payload and assert stability on the canonical export body. Update `backend/tests/snapshot_export.rs` to cover stable serialization, locked-field completeness, and re-hash behavior after export.</action>
  <acceptance_criteria>
    - `rg -n "pub struct WorkflowEvidenceExport|schema_version|hash_algorithm|root_hash|chain_verified|exported_at" backend/src/models/evidence.rs backend/src/services/evidence_graph.rs backend/tests/snapshot_export.rs`
    - `rg -n "context_snapshot|tool_invocations|llm_input_hash|llm_output_hash|attempt_id" backend/tests/snapshot_export.rs`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo test --test snapshot_export</automated>
  </verify>
  <done>The evidence service can emit a deterministic export payload that is complete enough for offline audit review and backed by automated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update architecture docs with guarantees, limits, and EU AI Act reporting examples</name>
  <read_first>
    - docs/ARCHITECTURE.md
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
    - backend/src/models/evidence.rs
    - backend/src/axum_routers/memory.rs
  </read_first>
  <files>docs/ARCHITECTURE.md, docs/evidence_graph.md</files>
  <action>Update `docs/ARCHITECTURE.md` and add `docs/evidence_graph.md` to describe the implemented evidence graph model, append-only node/edge storage, hash-chain verification flow, deterministic export contract, and the audit API at `GET /api/v1/workflows/{id}/evidence`. Include one concrete EU AI Act reporting example that maps stored fields to an audit narrative. Explicitly state the limits: the system is tamper-evident for stored data, but it does not independently validate external tool truthfulness, retention policy, or legal sufficiency. Do not describe unimplemented OpenTelemetry or UI work as already shipped.</action>
  <acceptance_criteria>
    - `test -f docs/evidence_graph.md`
    - `rg -n "Evidence Graph|GET /api/v1/workflows/\\{id\\}/evidence|tamper-evident|EU AI Act|limitations|chain_verified" docs/ARCHITECTURE.md docs/evidence_graph.md`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Evidence Graph|GET /api/v1/workflows/\\{id\\}/evidence|tamper-evident|EU AI Act|limitations" docs/ARCHITECTURE.md docs/evidence_graph.md</automated>
  </verify>
  <done>The docs match the implemented API and data model, explain guarantees and limits clearly, and provide the compliance-oriented example required by COMP-01.</done>
</task>

</tasks>

<verification>
Run `cd backend && cargo test --test evidence_api --test snapshot_export`. Then run `rg -n "Evidence Graph|GET /api/v1/workflows/\\{id\\}/evidence|EU AI Act|tamper-evident|limitations" docs/ARCHITECTURE.md docs/evidence_graph.md`.
</verification>

<success_criteria>
Phase 01 ends with a live audit API, deterministic export payloads, and documentation that accurately describes both the feature’s value and its boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-03-SUMMARY.md`
</output>
