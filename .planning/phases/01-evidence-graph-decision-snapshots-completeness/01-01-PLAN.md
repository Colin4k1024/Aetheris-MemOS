---
phase: 01-evidence-graph-decision-snapshots-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/evidence.rs
  - backend/src/models/mod.rs
  - backend/migrations/20260326000100_workflow_evidence_graph.sql
  - backend/migrations_sqlite/20260326000100_workflow_evidence_graph.sql
  - backend/tests/evidence_graph.rs
  - backend/tests/hash_chain.rs
autonomous: true
requirements:
  - EVID-01
  - EVID-02
must_haves:
  truths:
    - "Phase 01 establishes an explicit evidence contract that preserves all locked integrity fields required for auditable storage."
    - "Evidence persistence is anchored to append-only runs/nodes/edges so later API and export behavior can prove sequence integrity."
    - "A reusable Wave 0 verification baseline exists before repository and endpoint plans execute, reducing regressions in later waves."
  artifacts:
    - "backend/src/models/evidence.rs defines the evidence node, edge, verification, response, and export structs."
    - "backend/migrations/20260326000100_workflow_evidence_graph.sql creates append-only workflow evidence tables."
    - "backend/migrations_sqlite/20260326000100_workflow_evidence_graph.sql creates SQLite-compatible evidence tables for local runs."
    - "backend/tests/evidence_graph.rs and backend/tests/hash_chain.rs exist with runnable integration-test scaffolds."
  key_links:
    - "backend/src/models/evidence.rs is re-exported from backend/src/models/mod.rs so later plans can import stable contracts."
    - "The migration table/column names match the model field names and required locked fields."
    - "The test files import the new evidence contracts directly, preventing later plans from drifting from the agreed interface."
---

<objective>
Establish the evidence graph contracts, append-only schema, and Wave 0 test harness that later Phase 01 plans will build on.

Purpose: Lock the data shape and verification surface before repository logic or live API work starts.
Output: Typed evidence models, paired PostgreSQL/SQLite migrations, and runnable integration-test scaffolds for evidence graph and hash-chain behavior.
</objective>

<execution_context>
@/Users/jiafan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jiafan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-VALIDATION.md
@backend/src/services/scheduler.rs
@backend/src/db/decision_trace.rs
@backend/src/services/context_snapshot.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define evidence graph contracts and append-only schema</name>
  <read_first>
    - backend/src/services/scheduler.rs
    - backend/src/db/decision_trace.rs
    - backend/src/services/context_snapshot.rs
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
  </read_first>
  <files>backend/src/models/evidence.rs, backend/src/models/mod.rs, backend/migrations/20260326000100_workflow_evidence_graph.sql, backend/migrations_sqlite/20260326000100_workflow_evidence_graph.sql</files>
  <action>Create `backend/src/models/evidence.rs` with explicit structs for `WorkflowEvidenceRun`, `WorkflowEvidenceNode`, `WorkflowEvidenceEdge`, `WorkflowEvidenceVerification`, `WorkflowEvidenceResponse`, and `WorkflowEvidenceExport`. Implement the locked fields from D-01 and D-02 exactly: `timestamp`, `attempt_id`, `llm_input_hash`, `llm_output_hash`, `tool_invocations`, and `context_snapshot`. Use ordered data shapes for any free-form metadata (`BTreeMap` rather than `HashMap`) so later export hashing is deterministic. Re-export the module from `backend/src/models/mod.rs`. Create paired PostgreSQL and SQLite migrations that add append-only tables `workflow_evidence_runs`, `workflow_evidence_nodes`, and `workflow_evidence_edges`; include immutable sequence columns, `prev_hash`, `node_hash`, workflow/task identifiers, and JSON payload columns for tool invocations and context snapshots. Do not rename, drop, or repurpose the existing `decision_trace` table in this plan.</action>
  <acceptance_criteria>
    - `rg -n "pub struct WorkflowEvidenceNode|pub struct WorkflowEvidenceEdge|pub struct WorkflowEvidenceExport|pub struct WorkflowEvidenceResponse" backend/src/models/evidence.rs`
    - `rg -n "pub mod evidence|pub use evidence::\\*" backend/src/models/mod.rs`
    - `rg -n "CREATE TABLE IF NOT EXISTS workflow_evidence_runs|CREATE TABLE IF NOT EXISTS workflow_evidence_nodes|CREATE TABLE IF NOT EXISTS workflow_evidence_edges" backend/migrations/20260326000100_workflow_evidence_graph.sql backend/migrations_sqlite/20260326000100_workflow_evidence_graph.sql`
    - `rg -n "timestamp|attempt_id|llm_input_hash|llm_output_hash|tool_invocations|context_snapshot|prev_hash|node_hash" backend/migrations/20260326000100_workflow_evidence_graph.sql backend/migrations_sqlite/20260326000100_workflow_evidence_graph.sql`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo check --lib</automated>
  </verify>
  <done>The repository has a compileable evidence graph contract file and concrete migrations that encode the locked audit fields without breaking legacy decision-trace storage.</done>
</task>

<task type="auto">
  <name>Task 2: Add Wave 0 evidence graph and hash-chain test scaffolds</name>
  <read_first>
    - backend/src/models/evidence.rs
    - backend/src/services/scheduler.rs
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-VALIDATION.md
    - .planning/codebase/TESTING.md
  </read_first>
  <files>backend/tests/evidence_graph.rs, backend/tests/hash_chain.rs</files>
  <action>Create integration tests `backend/tests/evidence_graph.rs` and `backend/tests/hash_chain.rs` with runnable scaffolds that import the new evidence contracts. `evidence_graph.rs` must cover DTO serialization/deserialization, required-field presence, and append-only sequence expectations for nodes and edges. `hash_chain.rs` must define the expected tamper-detection behaviors for a broken `prev_hash` link, reordered sequence numbers, and a mutated `context_snapshot`. Keep at least one non-ignored assertion in each file so the listed test command passes immediately; only mark DB-dependent cases `#[ignore]` if later plans must fill them in.</action>
  <acceptance_criteria>
    - `test -f backend/tests/evidence_graph.rs`
    - `test -f backend/tests/hash_chain.rs`
    - `rg -n "#\\[test\\]|#\\[tokio::test\\]" backend/tests/evidence_graph.rs backend/tests/hash_chain.rs`
    - `rg -n "prev_hash|node_hash|context_snapshot|attempt_id" backend/tests/evidence_graph.rs backend/tests/hash_chain.rs`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo test --test evidence_graph --test hash_chain</automated>
  </verify>
  <done>Wave 0 coverage exists for EVID-01/EVID-02 storage contracts and EVID-03 hash-chain expectations, so later implementation plans have a stable test target.</done>
</task>

</tasks>

<verification>
Run `cd backend && cargo test --test evidence_graph --test hash_chain` after the plan completes. Confirm the migrations and tests mention every locked field and the append-only tables by name.
</verification>

<success_criteria>
Phase 01 has a stable contract baseline: typed evidence graph models, append-only migrations for both DB backends, and runnable integration-test scaffolds for graph storage and hash-chain verification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-01-SUMMARY.md`
</output>
