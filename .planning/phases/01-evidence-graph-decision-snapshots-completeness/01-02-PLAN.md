---
phase: 01-evidence-graph-decision-snapshots-completeness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/db/evidence_graph.rs
  - backend/src/db/mod.rs
  - backend/src/services/evidence_graph.rs
  - backend/src/services/mod.rs
  - backend/src/services/memory_orchestrator.rs
  - backend/tests/evidence_graph.rs
  - backend/tests/hash_chain.rs
autonomous: true
requirements:
  - EVID-01
  - EVID-02
  - EVID-03
must_haves:
  truths:
    - "Persisted decision traces are also stored as queryable evidence nodes and edges."
    - "Hash-chain verification recomputes canonical digests and fails when sequence integrity is broken."
    - "Legacy blob trace storage remains intact while the new append-only evidence path is added."
  artifacts:
    - "backend/src/db/evidence_graph.rs persists workflow runs, nodes, and edges."
    - "backend/src/services/evidence_graph.rs builds evidence records and verifies the stored chain."
    - "backend/src/services/memory_orchestrator.rs writes both the legacy trace blob and the new evidence graph."
  key_links:
    - "backend/src/services/memory_orchestrator.rs calls backend/src/services/evidence_graph.rs when trace persistence happens."
    - "backend/src/services/evidence_graph.rs depends on backend/src/db/evidence_graph.rs for append-only writes and ordered reads."
    - "backend/tests/hash_chain.rs exercises the same verification function used by production code."
---

<objective>
Implement the evidence graph persistence path and tamper-detection logic behind the Phase 01 contracts.

Purpose: Make evidence storage real before exposing it through the live HTTP surface.
Output: Repository and service modules that persist evidence runs, convert decision traces into append-only nodes/edges, and verify the resulting SHA-256 hash chain.
</objective>

<execution_context>
@/Users/jiafan/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jiafan/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
@.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
@backend/src/db/decision_trace.rs
@backend/src/services/memory_orchestrator.rs
@backend/src/services/scheduler.rs
@backend/src/models/evidence.rs
@backend/tests/evidence_graph.rs
@backend/tests/hash_chain.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement append-only evidence repository and canonical hash-chain service</name>
  <read_first>
    - backend/src/models/evidence.rs
    - backend/src/db/decision_trace.rs
    - backend/src/services/memory_orchestrator.rs
    - backend/tests/evidence_graph.rs
    - backend/tests/hash_chain.rs
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-RESEARCH.md
  </read_first>
  <files>backend/src/db/evidence_graph.rs, backend/src/db/mod.rs, backend/src/services/evidence_graph.rs, backend/src/services/mod.rs, backend/tests/evidence_graph.rs, backend/tests/hash_chain.rs</files>
  <behavior>
    - Test 1: Appending a decision trace creates an ordered run plus node and edge records with the locked evidence fields present.
    - Test 2: Recomputing the chain over stored nodes succeeds for unmodified data and fails when `prev_hash`, `node_hash`, or canonical snapshot bytes are changed.
    - Test 3: Listing a workflow run returns nodes in stable `seq_no` order with their directed edges intact.
  </behavior>
  <action>Add `backend/src/db/evidence_graph.rs` as the dedicated repository boundary recommended by research. Implement concrete methods for creating a workflow run, appending ordered nodes, appending edges, and listing a workflow's evidence in sequence order. For new DB operations, use SQLx compile-time checked query macros (`sqlx::query!` / `sqlx::query_as!`) where feasible rather than unchecked dynamic SQL paths. Add `backend/src/services/evidence_graph.rs` with functions that transform the current scheduler `DecisionTrace` into graph nodes/edges, compute `prev_hash` and `node_hash` with SHA-256 over canonical bytes, and verify an existing chain by replaying the same byte construction. Use ordered serialization inputs and fail closed on missing locked fields instead of silently omitting them. Register both modules in `backend/src/db/mod.rs` and `backend/src/services/mod.rs`. Update the two integration tests so they exercise the production repository/service code instead of only DTO scaffolding.</action>
  <acceptance_criteria>
    - `rg -n "pub async fn create_run|pub async fn append_nodes|pub async fn append_edges|pub async fn list_workflow_evidence" backend/src/db/evidence_graph.rs`
    - `rg -n "sqlx::query!|sqlx::query_as!" backend/src/db/evidence_graph.rs`
    - `rg -n "sha2|Sha256|verify_chain|record_decision_trace_as_evidence|build_evidence_nodes" backend/src/services/evidence_graph.rs`
    - `rg -n "pub mod evidence_graph" backend/src/db/mod.rs backend/src/services/mod.rs`
    - `rg -n "verify_chain|record_decision_trace_as_evidence" backend/tests/evidence_graph.rs backend/tests/hash_chain.rs`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo test --test evidence_graph --test hash_chain</automated>
  </verify>
  <done>The backend has a dedicated evidence repository/service pair that persists append-only evidence records and can detect tampering through canonical hash-chain replay.</done>
</task>

<task type="auto">
  <name>Task 2: Wire trace persistence into the live decision path without breaking legacy storage</name>
  <read_first>
    - backend/src/services/memory_orchestrator.rs
    - backend/src/db/decision_trace.rs
    - backend/src/services/evidence_graph.rs
    - backend/src/services/scheduler.rs
    - .planning/phases/01-evidence-graph-decision-snapshots-completeness/01-CONTEXT.md
  </read_first>
  <files>backend/src/services/memory_orchestrator.rs, backend/src/services/evidence_graph.rs, backend/tests/evidence_graph.rs</files>
  <action>Modify `persist_trace_record` and any related trace-persistence helpers in `backend/src/services/memory_orchestrator.rs` so that every persisted decision trace also writes a workflow evidence run and append-only evidence nodes/edges. Treat `task_id` as the Phase 01 workflow identifier unless the repository already exposes a stronger workflow concept. Keep the existing `decision_trace` blob insert for backward compatibility with `/api/v1/memory/traces`; add the evidence append after the legacy insert so Phase 01 ships incrementally instead of replacing the old path. Make the evidence service return enough metadata for the later API plan to query by workflow id and verification status.</action>
  <acceptance_criteria>
    - `rg -n "record_decision_trace_as_evidence|list_workflow_evidence|workflow_id" backend/src/services/memory_orchestrator.rs backend/src/services/evidence_graph.rs`
    - `rg -n "DecisionTraceRepository::create" backend/src/services/memory_orchestrator.rs`
    - `rg -n "persist_trace_record" backend/tests/evidence_graph.rs`
  </acceptance_criteria>
  <verify>
    <automated>cd backend && cargo test --test evidence_graph --test hash_chain</automated>
  </verify>
  <done>Persisting a trace now writes both the legacy trace blob and the new evidence graph records, giving later plans a live data source without regressing existing trace consumers.</done>
</task>

</tasks>

<verification>
Run `cd backend && cargo test --test evidence_graph --test hash_chain`. Inspect the test names and assertions to confirm they hit the production evidence service and not placeholder helpers.
</verification>

<success_criteria>
The backend can turn a stored `DecisionTrace` into append-only, hash-linked evidence graph records while preserving the existing blob trace path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-evidence-graph-decision-snapshots-completeness/01-02-SUMMARY.md`
</output>
